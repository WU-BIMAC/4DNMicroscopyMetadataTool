{
	"$schema": "http://json-schema.org/draft-07/schema",
	"id": "Car.json",
	"type": "object",
	"extends": {
		"type": "object",
		"javaType": "testPackage.Vehicle"
    },
	"title": "Car",
	"description": "A car is used for transportation.",
	"properties": {
		"engine": { 
			"type": "string", 
			"description": "The type of car engine." 
		}
	},
	"required": [ "engine" ]
}